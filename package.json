{
  "name": "caretaker24",
  "private": true,
  "scripts": {
    "start": "concurrently -n SERVER,MOBILE -c blue,green \"npm --prefix server run dev\" \"npm --prefix mobile run start\"",
    "dev": "concurrently -n SERVER,MOBILE -c blue,green \"npm --prefix server run dev\" \"npm --prefix mobile run start\"",
    "dev:server": "npm --prefix server run dev",
    "dev:mobile": "npm --prefix mobile run start",
    "build:android": "npm --prefix mobile run build:android",
    "build:ios": "npm --prefix mobile run build:ios",
    "install:all": "npm install --prefix server && npm install --prefix mobile"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
